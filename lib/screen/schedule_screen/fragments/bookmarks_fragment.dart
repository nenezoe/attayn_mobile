import 'package:flutter/material.dart';

class BookMarksFragments extends StatelessWidget {
  const BookMarksFragments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('nice one'),
        ],
      ),
    );
  }
}
