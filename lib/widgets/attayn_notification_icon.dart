import 'package:flutter/material.dart';

class AttaynNotificationIcon extends StatelessWidget {
  const AttaynNotificationIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
